# T-117 — SQL payee identity index

Status: **COMPLETE** (2026-08-02)

Files: `lib/data/db/tables/payee_evidence_table.dart`,
`lib/data/db/database.dart`, `lib/data/repositories/payee_label_repository.dart`,
`lib/features/settings/payee_labels_screen.dart`, and their focused tests.

Depends: none.

Change: Add the rebuildable schema-15 `payee_evidence` index from authoritative
merchant/VPA fields; maintain it during ingestion, corrections, migration, and
backup restore; execute payee aggregation, alias search, unlabeled filtering,
and keyset paging in SQL; and expose duplicate suggestions as review-only.

Tests: `flutter analyze`; focused repository, migration, and payee-label UI
tests; full `flutter test`; `git diff --check`; GitNexus `detect_changes`.

AC: Payee Settings never materializes the full eligible transaction history to
build its identity list; each page is bounded, search includes display names
and aliases, deleted/duplicate-linked transactions are excluded, and no
automatic alias or merchant merge occurs.

Privacy / rollback: The index stores normalized derived evidence and preserves
the source fields as authoritative. Rebuild or remove the derived table without
changing transactions or user labels; schema migration is additive.

Decision: [ADR 0010](../decisions/0010-sql-payee-identity-index.md).
