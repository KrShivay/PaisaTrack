# T-151 — Ask PaisaTrack design conformance

Divergences from handoff §07 are structural, not cosmetic. Do T-150a first so the
composer is built once.

### T-151a — Sheet presentation and single header   [P2] [~M]
Files:    `lib/features/assistant/assistant_screen.dart:89-120,124-209`
Depends:  T-152a
Change:   The title renders **twice** — once in the `AppBar` (line 106) and again
          in the header row (line 140). Drop the `Scaffold`/`AppBar` and present
          as a full-height dark sheet from the nav Ask orb. Surface is always
          `#0E0C1A` regardless of theme — remove the `isDark` branch on the
          background. One header: 34px mascot, "Ask PaisaTrack", an 11px emerald
          sub reading "On-device · no internet used" (replacing both the current
          "Natural language financial search" sub and the separate pill badge),
          and a ✕ at the right, over a 1px `#1E1B33` divider.
Tests:    Widget test asserting exactly one "Ask PaisaTrack" in the tree, and a
          dark surface under a light theme.
AC:       The title appears once; the sheet is dark in both themes; the orb opens
          it as a sheet, not a route.

### T-151b — Bubble geometry and verdict answers   [P2] [~S]
Files:    `lib/features/assistant/assistant_screen.dart:390-469`
Depends:  T-151a
Change:   Both radii are mirrored onto the wrong corners. User bubble: `#6D5AE6`,
          radius `20 20 6 20`, max width 78%, right-aligned. Assistant bubble:
          `#191630` + 1px `#262244`, radius `20 20 20 6`, max 88%. Add the
          verdict form — a 14px/600 emerald lead line ("Yes — with ₹3,100 to
          spare") followed by 13px `#B8B2D6` reasoning — driven by the existing
          renderer, not by model prose.
Tests:    Golden or widget test per bubble type asserting corner radii. Verdict
          test asserts the lead line comes from deterministic query fields.
AC:       Bubble tails point at their speaker. No model-authored number reaches
          the lead line.

### T-151c — Composer   [P2] [~S]
Files:    `lib/features/assistant/assistant_screen.dart:234-295`
Depends:  T-151a
Change:   52px input pill `#191630` + 1px `#2E2A4E`, placeholder `#6F6A92` "Ask
          anything about your money…", and a 40px **emerald gradient**
          (`#34D399 → #0E9F6E`) send button with an ink ↑ — currently a 36px flat
          violet circle with a white ↑.
Tests:    Widget test asserting send-button gradient and size.
AC:       Matches §07; hosts T-150c's chip row above it.

### T-151d — Thinking, model-missing, and no-answer states   [P2] [~M]
Files:    `lib/features/assistant/assistant_screen.dart:63-77,228-232`
Depends:  T-151b
Change:   Replace `BloomSkeleton` with three 6px `#6D5AE6` dots pulsing at 600ms
          stagger inside an assistant bubble. Replace the plain `LlmUnavailable`
          sentence with the gold bubble offering the model download, showing size
          and a progress bar. Replace the generic failure sentence with "I can't
          answer that offline yet" plus two reframing chips drawn from the
          catalogue. Respect reduce-motion by dropping the pulse.
Tests:    Widget test per state. Reduce-motion test asserts static dots.
AC:       All three terminal states render as designed; no raw exception text is
          ever shown.

### T-151e — Inline charts and follow-up chips   [P3] [~M]
Files:    `lib/features/assistant/assistant_screen.dart`,
          `lib/intelligence/assistant/answer_renderer.dart`
Depends:  T-151b
Change:   Optional inline chart inside an assistant bubble: 52px bars, 4px gaps,
          inactive `#3A3462`, the referenced category in its own hue, mono 10px
          axis labels, then a 1px `#262244` rule and a 12px `#A9A2CC`
          explanatory line. Follow-up chips beneath the bubble (32px, `#191630` +
          1px `#2E2A4E`); action chips use `#122B22`/`#1F5142`/`#7FE8C4`.
Tests:    Widget test: a breakdown answer renders bars whose values equal the
          query result; an action chip performs its action.
AC:       Every charted number comes from `AssistantQueryEngine`, never from the
          model.
Rationale: `docs/ui-gaps-and-redesign.md` §U6 has the full divergence table.
