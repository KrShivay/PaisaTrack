### T-160a — Replace Activity's limit heuristic with a page contract  [P0] [~M]
Files:    `lib/data/repositories/transaction_repository.dart:163`,
          `lib/features/transactions/transactions_providers.dart:24`,
          `lib/features/transactions/transactions_screen.dart:40,374`,
          `test/data/repositories/transaction_repository_test.dart`,
          `test/features/transactions/transactions_screen_test.dart`
Depends:  none; T-160b consumes this page contract
Change:   Return an explicit Activity page object containing the visible rows,
          `hasMore`, and the continuation cursor shape. Fetch one look-ahead
          row so the UI never infers continuation from `rows.length == limit`.
          Keep the current Activity filters and preserve stable page snapshots;
          keyset cursor application is T-160b.
Tests:    Repository coverage for an exactly-100 terminal history, a history
          with a 101st row, and suppressed/deleted rows. Widget coverage for
          hiding the continuation action when `hasMore` is false and showing
          it when true.
AC:       An Activity history with exactly 100 visible rows renders no false
          continuation action; a history with more visible rows renders one;
          no deleted or duplicate-suppressed row is exposed; no raw SMS body is
          introduced into the page model.
Rationale: `TASKS.md` T-160a — the current limit heuristic cannot distinguish
           a full terminal page from a truncated page.

### T-160b — Add Activity keyset pagination by `(ts, id)`  [P0] [~M]
Files:    `lib/data/repositories/transaction_repository.dart`,
          `lib/features/transactions/transactions_providers.dart`,
          `lib/features/transactions/transactions_screen.dart`,
          `test/data/repositories/transaction_repository_test.dart`,
          `test/features/transactions/transactions_screen_test.dart`,
          `test/features/transactions/transactions_page_controller_test.dart`
Depends:  T-160a
Change:   Use the page continuation cursor as a strict `(ts, id)` keyset so
          Activity can reach older records without offset drift or duplicates.
          Preserve newest-first ordering and the existing visibility predicate;
          page snapshots keep a live insert from moving an already-fetched
          boundary during continuation.
Tests:    Seed 1,000 mixed-date transactions, including identical timestamps,
          deleted rows, duplicate-suppressed rows, and inserts between page
          reads; assert every visible id is reachable once and only once.
AC:       Paging reaches all visible transactions without gaps or duplicates,
          remains stable when timestamps tie, and does not require loading the
          complete transaction history into Dart.
Rationale: `TASKS.md` T-160b — offset/limit growth is not a stable history
           cursor when rows are inserted or filtered between reads.

### T-160c — Preserve Activity search/filter continuation  [P1] [~S]
Files:    `lib/features/transactions/transactions_screen.dart`,
          `test/features/transactions/transactions_screen_test.dart`
Depends:  T-160b
Change:   Keep the Activity continuation action available when the active
          search/filter has no match in the currently loaded page but the page
          contract reports `hasMore`. Loading another page must preserve the
          existing query and filter state.
Tests:    Widget coverage starts with a non-matching loaded page, loads more,
          and asserts an older matching row becomes visible while the query
          remains active.
AC:       A user can search for a transaction that is not in the loaded page,
          continue paging, and see the matching older row without clearing or
          reapplying the search/filter.
Rationale: `TASKS.md` T-160c — client-side filtering must not make older
           matching records unreachable.
