# T-150 — Assistant prompt catalogue

T-150a–c status: **COMPLETE** (2026-08-06–07).

Before T-150a, the assistant rendered 4 hardcoded presets from a 44-question
catalogue; two had no supported intent and always refused.

### T-150a — Extract the catalogue and test it against the validator   [P2] [~M]
Files:    `lib/intelligence/assistant/prompt_catalogue.dart` (new),
          `lib/features/assistant/assistant_screen.dart:303-388` (remove
          `_PresetQuestions`' hardcoded list)
Depends:  none
Recover:  `git show 5207874^:lib/features/assistant/assistant_screen.dart`
          — `_allCategories`, 7 groups: Spending 8, Breakdowns 6,
          Subscriptions & Bills 6, Income & Savings 6, Trends & Compare 6,
          Insights & Alerts 5, Merchants & Fees 7.
Change:   Move the catalogue into a plain Dart module (group id, label, icon,
          questions) so it is testable without a widget. Both the empty state and
          the composer chips read from this one source.
Tests:    **The load-bearing test:** every catalogue entry validates to a
          supported `AssistantIntentKind` via `IntentValidator`. Remove or
          rewrite entries that fail — including the current presets "Can I afford
          a ₹5,000 dinner?" and "Show food vs shopping", which have no matching
          intent.
AC:       No shipped suggestion can refuse for lack of a supported intent. The
          catalogue and classifier cannot drift apart silently again.

### T-150b — Searchable empty state   [P2] [~M]
Files:    `lib/features/assistant/assistant_screen.dart`
Depends:  T-150a
Change:   Replace the four-tile empty state with a search field over all
          catalogue entries, results grouped under their group headers, showing
          all 44 when the query is empty. Case-insensitive substring match across
          question text and group label.
Tests:    Widget test: empty query shows every group; "subscription" surfaces the
          Subscriptions & Bills group; no match shows a clear empty message.
AC:       Every catalogue question is reachable without scrolling a flat list.

### T-150c — Rotating composer chips   [P3] [~S]
Files:    `lib/features/assistant/assistant_screen.dart`
Depends:  T-150a, T-151c (composer container)
Change:   Per handoff §07: a horizontally scrollable 30px chip row above the
          input, `#191630` / `#A9A2CC`, three visible at a time, rotating through
          the catalogue rather than rendering a full tray.
Tests:    Widget test: three chips visible; tapping one sends that question;
          rotation changes the set.
AC:       Suggestions remain reachable once a conversation has started, not only
          on the empty state.
Rationale: `docs/ui-gaps-and-redesign.md` §U5.
