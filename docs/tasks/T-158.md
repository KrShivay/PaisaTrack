# T-158 — Split transaction_detail_screen.dart

1,352 lines mixing a data provider, ML inference orchestration, an
optimistic-update controller (T-157b), five pure formatting/classification
functions, and eight-plus widget classes. Split along those seams; no rendered
output changes.

### T-158a — Extract pure functions   [P2] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart:262-292`
          (chip ranking), `:452-507` (exclusion-reason classification),
          `:892-914` (`_formatDate`/`_shortMonth`), `:956-1100` (evidence-span
          slicing/highlight mapping), `:1247-1258` (parser-source label +
          confidence formatting); new
          `lib/features/transactions/detail/transaction_detail_formatting.dart`
          and `transaction_detail_evidence.dart`
Depends:  none
Change:   Extract each block into a standalone, non-widget, pure function:
          `chipCategories(...)`, `exclusionReasonFor(Transaction)`,
          `formatDetailDate(DateTime)`,
          `buildEvidenceSpans(rawSmsBody, evidence, isDark)`,
          `parserSourceLabel(String?)`. Replace each `build()` call site with a
          call to the extracted function. Fold `exclusionReasonFor`'s
          classification into the same predicate
          `dashboard_repository.dart:156` uses for its SQL `LIKE` filter (or
          extract that predicate to a shared Dart function both can call), so
          "what counts as excluded" has one definition instead of two
          independently-maintained ones.
Tests:    New unit tests per extracted function (T-159b) covering branches
          currently only reachable through widget pumps: multi-suggestion
          dedup/fill, each exclusion-reason category, evidence spans at span
          boundaries, purged/retained/manual-entry states, all four
          parser-source labels.
AC:       Each function is unit-testable without pumping a widget; the screen
          renders byte-identical output for the same inputs.
Rationale: none of this logic is reachable today by a test that doesn't also
          pump the full screen.

### T-158b — Extract TransactionDetailController   [P2] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart:63-220`
          (`_TransactionDetailScreenState` fields, `_seed`, `_saveNote`), new
          `lib/features/transactions/detail/transaction_detail_controller.dart`
Depends:  T-157b (shared correction controller must exist first)
Change:   Move note-text seeding/save state and the calls into T-157b's shared
          correction controller out of `_TransactionDetailScreenState` into a
          dedicated Riverpod notifier keyed by transaction id, replacing the ad
          hoc `_seed(detail)` called mid-`build()` with proper notifier
          initialization. The screen's `State` class becomes a thin consumer.
Tests:    The existing note-save round-trip test
          (`transaction_detail_correction_test.dart:97`) must keep passing
          against the new controller; add a test asserting seeding no longer
          happens inside `build()`.
AC:       `_TransactionDetailScreenState` no longer owns `_seed`/`_saveNote`
          logic directly.
Rationale: mutating instance fields mid-`build()` breaks under
          hot-reload/rebuild churn; a notifier initializer is the standard fix.

### T-158c — Extract sub-widgets into detail/   [P2] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart:1103-1352`
          (`_InlineCategoryChip`, `_MoreCategoryChip`,
          `_WhereThisCameFromSection`), `:917-1101`
          (`_SourceMessageEvidenceView`), plus the inline metadata card,
          exclusion banner, review-queue banner, note editor, and
          technical-details disclosure currently built inline in `build()`;
          new files under `lib/features/transactions/detail/`
Depends:  T-158a (widgets should consume the extracted pure functions, not
          re-derive them)
Change:   Move each self-contained widget class to its own file, and extract
          the metadata card / exclusion banner / review-queue banner / note
          editor / technical-details block into named widget classes in the
          same directory. File/class-boundary move only — no visual change.
Tests:    Existing render-focused tests (`transaction_detail_screen_test.dart`)
          pass unchanged against the new file layout.
AC:       `build()` reads as a composition of named widgets; no widget's
          rendered tree changes.
Rationale: this sub-task is the actual line-count reduction the whole
          initiative is scoped around.

### T-158d — Verify exclusion-reason parity between UI and analytics   [P3] [~S]
Files:    `lib/features/transactions/detail/transaction_detail_formatting.dart`
          (from T-158a), `lib/data/repositories/dashboard_repository.dart:156`
Depends:  T-158a
Change:   Once T-158a extracts `exclusionReasonFor` as a standalone function,
          write a parity test asserting that every transaction
          `dashboard_repository.dart`'s SQL `LIKE` filter excludes from
          analytics also classifies as excluded by `exclusionReasonFor`, and
          vice versa, across fixtures covering CREDIT CARD, CARD BILL, ATM, and
          ordinary spends. If a mismatch is found, do not silently fix it —
          file a new ticket; resolving a real behavioral discrepancy is out of
          scope for a no-behavior-change refactor.
Tests:    The parity test itself.
AC:       The parity test passes, or a follow-up ticket documents exactly
          where the two classifiers diverge.
Rationale: two independently-maintained implementations of "what counts as
          excluded" have never been compared by a test.
