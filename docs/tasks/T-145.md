# T-145 — Category picker full screen

### T-145a — Move the picker to the full-screen route   [P2] [~M]
Files:    `lib/core/widgets/category_picker_sheet.dart:57-66`
          callers: `transaction_detail_screen.dart:87`,
          `weekly_review_screen.dart` (recategorize path)
Depends:  T-152a
Change:   Replace `ConstrainedBox(maxHeight: 640)` and the bottom-sheet
          presentation with `showBloomFullScreenSheet`. Keep the existing
          Suggested / Recent / All sections, `_hierarchicalCategories` ordering,
          and the parent-name subtitles — those behaviours are correct. Keep
          swipe-down dismissal so it still reads as a sheet.
Tests:    Widget test at 402×874 with the keyboard inset simulated: at least 8
          result rows remain visible. Existing picker tests must pass unchanged.
AC:       The result list stays usable with the keyboard open; suggestion,
          recency, and hierarchy behaviour is byte-identical.

### T-145b — Sticky search and result affordances   [P3] [~S]
Files:    `lib/core/widgets/category_picker_sheet.dart`
Depends:  T-145a
Change:   Pin the search field to the top while the list scrolls; make section
          headers (`_SectionLabel`) sticky; show a result count next to the
          "Matches" header. Keep `autofocus: true`.
Tests:    Widget test: scrolling the list keeps the search field visible; the
          count matches the filtered length.
AC:       Searching a large hierarchical taxonomy no longer requires scrolling
          back up to change the query.
