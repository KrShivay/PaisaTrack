# T-146 — Category icons

Highest-visibility defect in the app; the data is already loaded and dropped.

### T-146a — Pass and resolve the category icon   [P1] [~S]
Files:    `lib/core/widgets/bloom/bloom_category_tile.dart:41`
          call sites: `dashboard_widgets.dart:698,1037`,
          `transactions_screen.dart:614`, `weekly_review_screen.dart:515,770`,
          `insights_screen.dart:714`, `transaction_detail_screen.dart:185`
Depends:  none
Change:   `BloomCategoryTile` resolves its glyph via
          `CategoryVisuals.icon(iconName)`; no call site passes `iconName`, so
          every row renders `Icons.category_outlined`. (1) Pass `iconName` at
          each call site from the already-available `categoryIcon` —
          `TransactionRepository` populates it at lines 843 and 880. (2) Change
          the tile to fall back to the icon mapped from `categoryId` when
          `iconName` is null, so a future missed parameter degrades to the right
          glyph instead of the generic one.
Tests:    Widget test per rendering surface asserting a **specific** glyph
          (a food row renders `Icons.restaurant`). Assert-not-null is
          insufficient — that is why this shipped.
AC:       No surface renders `fallbackIcon` for a seeded category. Hues are
          unchanged.
Impact:   Run `impact({target: "BloomCategoryTile", direction: "upstream"})`
          first — eight call sites across six screens.

### T-146b — Fix the recurring screen's category id   [P2] [~S]
Files:    `lib/features/recurring/recurring_screen.dart:407`
Depends:  none (independent of T-146a)
Change:   The tile receives `categoryId: series.kind`, where `kind` is a
          recurrence classification (`subscription`, `emi`), not a category id —
          so `CategoryVisuals.color` falls through to `fallbackColor`. Resolve
          the series' actual category, or render a kind-specific visual if the
          series has no single category.
Tests:    Widget test asserting a recurring row for a food merchant carries the
          food hue, not slate.
AC:       Recurring rows are tinted consistently with the same merchant's rows
          elsewhere in the app.
