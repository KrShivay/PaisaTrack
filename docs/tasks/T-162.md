### T-162a — Prove salary-credit ingestion end to end  [P0] [~S]

Files:    `test/fixtures/salary_credit_matrix.json`,
          `test/capture/salary_credit_ingestion_test.dart`, existing parser,
          categorizer, and ingestion contracts if a failing matrix case finds
          a production gap
Depends:  T-161b; no device evidence required for sanitized fixture coverage
Change:   Exercise representative supported HDFC and ICICI bank-header
          templates plus a sender-agnostic generic salary-credit message
          through the shipped `ParserCascade`, `Categorizer`, and
          `SmsIngestor`. Fixtures contain synthetic account tails and bodies
          only; no personal or copied bank-message content is retained.
Tests:    Every matrix case creates exactly one transaction with credit
          direction, the expected amount, and `income_salary` category. The
          existing generic/template parser suites remain green.
AC:       Supported template and generic salary-credit messages are tracked as
          income transactions, never debit or unparsed rows, and the matrix
          can be expanded without duplicating ingestion setup.
Rationale: `TASKS.md` T-162a — salary-credit messages were not proven across
           the same end-to-end path used by live/history capture, despite the
           parser and category seed contracts existing independently.

Verification: salary-credit and bank-parser focused suite **23/23**; fixture
matrix is non-empty and unique, template cases assert exact template IDs, and
the generic case uses an unsupported sender and asserts no template ID.
`flutter analyze --no-pub`, full Flutter verification, and GitNexus change
detection are recorded in `WORKLOG.md` after the task audit.
