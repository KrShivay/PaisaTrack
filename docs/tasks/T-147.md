# T-147 — Source message view

Handoff §08 specifies this and it was never built; the raw body is invisible in
every build.

### T-147a — Render the source message   [P2] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart:523-585`
          (replaces the "Technical details & SMS provenance" disclosure),
          `lib/data/repositories/raw_sms_repository.dart`
Depends:  T-152a
Change:   Load the stored body for the transaction's `smsId` and render it as a
          first-class section, not behind a disclosure: 12px/700 label "WHERE
          THIS CAME FROM", block at radius 18 `#F1FBF6` + 1px `#C9EEDD`, body in
          mono 11px `#4E7A69` at 1.6 line-height. Show the body **as stored** —
          it already carries parse-time account masking; do not re-derive or
          unmask. Handle the missing case as normal, not an error: `raw_sms` is
          purged after `AppConstants.rawSmsRetentionDays` (30), so render
          "Original message no longer stored — kept for 30 days".
Tests:    Widget test with a retained row (body renders), with a purged row
          (explanatory copy, no error), and with `smsId == null` (manual entry —
          section absent). Assert nothing unmasks the stored text.
AC:       The body is visible in release builds when retained and degrades
          cleanly after retention. Existing `kDebugMode` gating of
          `confidenceJson` is unchanged.

### T-147b — Provenance badge and privacy gating   [P3] [~S]
Files:    `lib/features/transactions/transaction_detail_screen.dart`
Depends:  T-147a
Change:   Add the 24px `#0E7A56` "Parsed locally" badge and an 11px line naming
          the parser and confidence ("Template match · 99%"), sourced from
          `parseSource` and `parseConfidence`. Mark the section so it is excluded
          from any future home widget (T-091) and covered by app lock (T-090) —
          record the requirement in `docs/privacy.md`.
Tests:    Widget test asserting the badge reflects `parseSource` for template,
          generic, LLM, and manual rows.
AC:       The most sensitive string in the app is documented as lock-covered and
          widget-excluded before either feature ships.
Rationale: `docs/ui-gaps-and-redesign.md` §U3. T-131's evidence spans later add
          highlighting inside this same block — build the container now.
