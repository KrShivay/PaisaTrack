# T-142 — Explain this charge

### T-142a — Highlight evidence spans in the source message   [P3] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart`
Depends:  T-131a evidence spans, T-147a source-message container
Change:   Inside the "WHERE THIS CAME FROM" block T-147a builds, highlight the
          exact substrings the amount, direction, and date were read from, using
          the stored `FieldEvidence` offsets. Name the parser and confidence per
          field.
Tests:    A template-parsed row highlights the amount span; a row whose
          `raw_sms` has been purged degrades to the T-147a copy without error;
          a pre-T-131 row with null evidence shows the body without highlights.
AC:       This is the user-facing proof of the numeric trust boundary — tapping
          any amount shows where it came from. Nothing is shown after retention.
Rationale: `docs/sms-intelligence-design.md` §9 Tier 3.
