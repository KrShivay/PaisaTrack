# T-148 — Category row tap target

### T-148a — Make the whole category row the control   [P1] [~S]
Files:    `lib/features/transactions/transaction_detail_screen.dart:243-297`
Depends:  none
Change:   The category name and tile are inert; the only target is the 12×6
          "Change" pill at line 273. Wrap tile + name + a trailing chevron in one
          `InkWell` with a ≥48dp hit area calling `_changeCategory`, and delete
          the pill. Add `Semantics(label: 'Category, <name>, double tap to
          change')`.
Tests:    Widget test: tapping the category name opens the picker. Semantics test
          asserts the label. Touch-target test asserts ≥48dp.
AC:       Tapping anywhere on the row opens the picker; the pill is gone;
          TalkBack announces the row as changeable.

### T-148b — Inline category chips in the detail sheet   [P2] [~M]
Files:    `lib/features/transactions/transaction_detail_screen.dart`
Depends:  T-148a, T-145a (the "More…" chip target)
Change:   Per handoff §08: a 12px/700 "CATEGORY" label above a chip row — the
          selected chip in the full category hue with white text (34px, radius
          17), alternatives in `#F1EFFB`/`#5B5580`, and a final "More…" chip
          opening the picker. Alternatives come from the classifier's ranked
          suggestions; cap at three plus "More…".
Tests:    Widget test: selected chip uses the category hue; tapping an
          alternative writes the correction without opening a sheet; "More…"
          opens the picker.
AC:       The common case (one of three likely categories) is a single tap with
          no sheet.
Rationale: `docs/ui-gaps-and-redesign.md` §U4.
